# simple-naive-ollama-benchmark

Very simple script for getting startup time/tokens per second from an ollama instance.

This does nothing to reflect how many models might be run in parallel and takes no account of the amount of RAM available on the GPUs.
